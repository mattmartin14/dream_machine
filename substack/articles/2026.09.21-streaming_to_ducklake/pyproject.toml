[project]
name = "flink-ducklake-demo"
version = "0.1.0"
description = "Kafka -> Flink dedup -> DuckLake demo pipeline"
requires-python = ">=3.13"
dependencies = [
    "confluent-kafka>=2.5.0",
    "duckdb>=1.1.0",
    "faker>=28.0.0",
    "pyarrow>=17.0.0",
]

[tool.uv]
package = false
