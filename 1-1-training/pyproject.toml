[project]
name = "sandbox-mateo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb==1.5.0",
]

[tool.uv.workspace]
members = [
    "module_2",
]
